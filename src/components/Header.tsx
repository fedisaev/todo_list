import {FC} from 'react';
import {HeaderStyles} from "../styles/StylesHeader";

const Header:FC = () => {
    return (
        <HeaderStyles>
            TODO LIST
        </HeaderStyles>
    );
};

export default Header;